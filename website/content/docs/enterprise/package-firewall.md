---
title: "Package Firewall"
weight: 3
description: "Configure package managers to use the Vulnetix Package Firewall for dependency policy enforcement."
---

The Vulnetix Package Firewall sits between your package manager and upstream registries. It authenticates your organization, checks dependency policy, and proxies allowed packages from trusted mirrors at `https://packages.vulnetix.com`.

## Ecosystems

| Ecosystem | Proxy URL | CLI setup |
| --- | --- | --- |
| Go | `https://packages.vulnetix.com` | `vulnetix package-firewall go` |
| npm | `https://packages.vulnetix.com/npm` | `vulnetix package-firewall npm` |
| PyPI | `https://packages.vulnetix.com/pypi` | `vulnetix package-firewall pypi` |
| Cargo | `https://packages.vulnetix.com/cargo` | `vulnetix package-firewall cargo` |
| RubyGems | `https://packages.vulnetix.com/gem` | `vulnetix package-firewall gem` |
| Hex | `https://packages.vulnetix.com/hex` | `vulnetix package-firewall hex` |
| pub.dev | `https://packages.vulnetix.com/pub` | `vulnetix package-firewall pub` |
| Maven | `https://packages.vulnetix.com/maven` | `vulnetix package-firewall maven` |
| NuGet | `https://packages.vulnetix.com/nuget` | `vulnetix package-firewall nuget` |
| Composer | `https://packages.vulnetix.com/composer` | `vulnetix package-firewall composer` |
| Conan | `https://packages.vulnetix.com/conan` | `vulnetix package-firewall conan` |
| Conda | `https://packages.vulnetix.com/conda` | `vulnetix package-firewall conda` |
| CRAN | `https://packages.vulnetix.com/cran` | `vulnetix package-firewall cran` |
| Julia | `https://packages.vulnetix.com/julia` | `vulnetix package-firewall julia` |
| Docker / OCI | `https://packages.vulnetix.com/v2` | `vulnetix package-firewall docker` |
| Debian / Ubuntu | `https://packages.vulnetix.com/debian` | `vulnetix package-firewall debian` |
| RPM | `https://packages.vulnetix.com/rpm` | `vulnetix package-firewall rpm` |
| Alpine | `https://packages.vulnetix.com/alpine` | `vulnetix package-firewall alpine` |
| Helm | `https://packages.vulnetix.com/helm` | `vulnetix package-firewall helm` |
| Chef | `https://packages.vulnetix.com/chef` | `vulnetix package-firewall chef` |
| Terraform | `https://packages.vulnetix.com/terraform` | `vulnetix package-firewall terraform` |

## Go Setup

Run:

```bash
vulnetix package-firewall go
```

The command:

- verifies your Vulnetix CLI credentials
- derives an API key when your CLI is authenticated with SigV4
- writes Basic auth credentials to `.netrc`
- persists `GOPROXY=https://packages.vulnetix.com`
- persists `GOAUTH=netrc`
- updates detected project files at the git root: `.env`, `.envrc`, and `Makefile`

Preview changes without writing files:

```bash
vulnetix package-firewall go --dry-run
```

## Language Registry Setup

The CLI reuses your existing Vulnetix authentication, writes the shared Package Firewall `.netrc` credentials, then updates the package manager's registry configuration.

```bash
vulnetix package-firewall npm
vulnetix package-firewall pypi
vulnetix package-firewall cargo
vulnetix package-firewall gem
vulnetix package-firewall hex
vulnetix package-firewall pub
vulnetix package-firewall maven
vulnetix package-firewall nuget
vulnetix package-firewall composer
vulnetix package-firewall conan
vulnetix package-firewall cran
vulnetix package-firewall helm
```

Use `--dry-run` with any setup command to preview file changes.

```bash
vulnetix package-firewall npm --dry-run
vulnetix package-firewall pypi --dry-run
```

Use `--proxy-url` for staging or self-hosted proxy testing:

```bash
vulnetix package-firewall npm --proxy-url https://packages.vulnetix.com
```

The setup commands manage these files:

| Ecosystem | Config file |
| --- | --- |
| npm | `~/.npmrc` |
| PyPI | `~/.config/pip/pip.conf` |
| Cargo | `~/.cargo/config.toml` |
| RubyGems | `~/.gemrc` |
| Hex | `~/.config/vulnetix/package-firewall/hex.env` |
| pub.dev | `~/.config/vulnetix/package-firewall/pub.env` |
| Maven | `~/.m2/settings.xml` |
| NuGet | `~/.nuget/NuGet/NuGet.Config` |
| Composer | `~/.composer/config.json` |
| Conan | `~/.conan2/remotes.json`, `~/.conan2/credentials.json` |
| CRAN | `~/.Rprofile` |
| Helm | `~/.config/helm/repositories.yaml` |

## Authentication

Go reads credentials from `.netrc` when `GOAUTH=netrc` is set. Other clients either use `.netrc` or their package-manager-specific auth config generated by the CLI.

Linux and macOS:

```netrc
machine packages.vulnetix.com
login your-organization-uuid
password your-api-key-hex
```

```bash
chmod 600 ~/.netrc
```

Windows:

```text
%USERPROFILE%\_netrc
```

Use the same machine entry:

```netrc
machine packages.vulnetix.com
login your-organization-uuid
password your-api-key-hex
```

## Manual Go Configuration

The setup command writes the persistent equivalent of:

```bash
export GOPROXY="https://packages.vulnetix.com"
export GOAUTH="netrc"
```

Fish:

```fish
set -gx GOPROXY https://packages.vulnetix.com
set -gx GOAUTH netrc
```

PowerShell:

```powershell
Add-Content $PROFILE '$env:GOPROXY = "https://packages.vulnetix.com"'
Add-Content $PROFILE '$env:GOAUTH = "netrc"'
```

CMD:

```cmd
setx GOPROXY "https://packages.vulnetix.com"
setx GOAUTH "netrc"
```

## Project Files

When run inside a git repository, the CLI updates existing files only:

`.env`

```dotenv
GOPROXY=https://packages.vulnetix.com
GOAUTH=netrc
```

`.envrc`

```bash
export GOPROXY="https://packages.vulnetix.com"
export GOAUTH="netrc"
```

`Makefile`

```make
export GOPROXY=https://packages.vulnetix.com
export GOAUTH=netrc
```

## Verify

```bash
vulnetix auth status
go env GOPROXY GOAUTH
go list -m all
```

`auth status` reports stored credentials, the Package Firewall `.netrc` entry, and detected package-manager configs such as `npm`, `pypi`, or `maven`.

If `auth status` reports that `.netrc` permissions are too open, run:

```bash
chmod 600 ~/.netrc
```
